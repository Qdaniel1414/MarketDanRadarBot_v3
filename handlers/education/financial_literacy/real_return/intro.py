from telegram import Update
from telegram.ext import ContextTypes


async def real_return_intro(
    update: Update,
    context: ContextTypes.DEFAULT_TYPE,
):
    await update.message.reply_text(
        """
📊 بازده واقعی (Real Return)

بازده واقعی یعنی:

میزان سود سرمایه‌گذاری
بعد از در نظر گرفتن تورم.

━━━━━━━━━━━━━━━━━━

فرمول:

بازده واقعی ≈
بازده اسمی − نرخ تورم

━━━━━━━━━━━━━━━━━━

مثال:

📈 سود سرمایه‌گذاری:

40٪

📉 تورم:

25٪

بازده واقعی:

15٪

━━━━━━━━━━━━━━━━━━

یعنی:

اگر سرمایه شما

40 درصد رشد کند

ولی تورم

25 درصد باشد،

قدرت خرید واقعی شما

فقط

15 درصد

بیشتر شده است.

📌 وارن بافت گفته است:

«سود واقعی چیزی است که بعد از تورم برای شما باقی می‌ماند.»
"""
    )